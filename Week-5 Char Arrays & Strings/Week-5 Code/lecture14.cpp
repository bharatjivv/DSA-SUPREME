#include <iostream>
using namespace std;

int main(){
    // Remove duplicate in a string
    /*
    string s = {'a', 'b', 'b', 'a', 'c', 'a'};
    string ans = "";

    int i = 0;
    while(i<s.length()){

        if(ans.length>0 && ans[ans.length -1] == s[i]) 
            ans.pop_back();
        else{
            ans.push_back(s[i]);
        }
    }

    */

   


    
    return 0;
}